CREATE TABLE `RECORDATORIO` (
  `ID_RECORDATORIO` INT,
  `ID_INBOX` INT,
  `RECORDATORIO` VARCHAR,
  `FECHA` DATE,
  PRIMARY KEY (`ID_RECORDATORIO`)
);

CREATE TABLE `CALENDARIO` (
  `ID_CALENDARIO` INT,
  `ID_NEXTACTIONS` INT,
  `ID_RECORDATORIO` INT,
  `ID_ACTIVIDAD` INT,
  PRIMARY KEY (`ID_CALENDARIO`)
);

CREATE TABLE `INCUBADORA` (
  `ID_INCUBADORA` INT,
  `` <type>,
  PRIMARY KEY (`ID_INCUBADORA`)
);

CREATE TABLE `NEXT ACTIONS` (
  `ID_NEXTACTIONS` INT,
  `ID_INCUBADORA` INT,
  `ID_ACTIVIDAD` INT,
  PRIMARY KEY (`ID_NEXTACTIONS`)
);

CREATE TABLE `TICKLER FILE` (
  `ID_TICKLER` INT,
  `ID_USER` INT,
  `COSAS_POR_HACER` VARHCAR,
  PRIMARY KEY (`ID_TICKLER`)
);

CREATE TABLE `ACTIVIDAD` (
  `ID_ACTIVIDAD` INT,
  `ID_USUARIO` INT,
  `TITULO` VARCHAR,
  `FECHA` DATE,
  `TIEMPO_INICIO` DATETIME,
  `TIEMPO_FINAL` DATETIME,
  `DETALLES` VARCHAR,
  `PRIORIDAD` VARCHAR,
  PRIMARY KEY (`ID_ACTIVIDAD`)
);

CREATE TABLE `INBOX` (
  `ID_INBOX` INT,
  `ID_ACTIVIDAD` INT,
  `ID_CALENDARIO` INT,
  `ID_USUARIO` INT,
  `ID_TICKLER` INT,
  `ID_INCUBADORA` INT,
  `IDEAS_DIARIAS` VAFCHAR,
  PRIMARY KEY (`ID_INBOX`)
);

CREATE TABLE `USUARIOS` (
  `ID_USUARIO` INT,
  `USUARIO` VARCHAR,
  `EMAIL` VARCHAR,
  `PASSWORD` VARCHAR,
  `CELULAR` VARCHAR,
  PRIMARY KEY (`ID_USUARIO`)
);

