import 'package:flutter/material.dart';

class Nosotros extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(
        title: new Text("Este es el servicio de soproto fuera de servicio"),
      ),
      body: new Center(
        child: new Text('Estamos en ello grasias :D'),
      ),
    );
  }
}
